<%= form_for(current_user.relationships.find_by(followed_id: @user.id), 
												html: {method: :delete},
												remote: true) do |f| %>
	<div class="row">
		<div class="form-group col-md-4 col-sm-12">
			<%= f.submit "Unfollow", class: "btn btn-large btn-default form-control" %>
		</div>
	</div>
<% end %>