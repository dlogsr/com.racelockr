<%= provide(:title, @title) %>
<div class="row">
	<aside class="span3">
		<section>
			<%= gravatar_for @user %>
			<h1>
				<%= link_to @user.name, @user %>
			</h1>
			<h2>
				<%= @user.email %>
			</h2>
		</section>
		<section>
			<%= render 'shared/stats' %>
		</section>
	</aside>
	<div class="span8">
	<%= render 'follow_form' if signed_in? %>
		<h3>Races (<%= @user.races.count %>)</h3>
		<%= render 'shared/race_feed' %>
	</div>
</div>
